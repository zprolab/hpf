[project]
name = "hpf"
description = "A high precision floating-point arithmetic module"
version = "0.3.5"
authors = [
    { name = "Chenyun Z.", email = "zcy@hi-zcy.com" }
]
dependencies = []

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"